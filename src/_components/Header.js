import Navigation from "./Navigation";
import SiteBrand from "./SiteBrand";
const Header = () => {
  return (
    <header>
      <SiteBrand />
      <Navigation />
    </header>
  );
};

export default Header;
